fun main() {

    var i = 1                 // Declaring and initializing a variable (i)
                              //The below code will loop until the condition becomes false
    while (i <= 5) {
        println("Number $i") //prints the output
        ++i                   // increments the value by 1
    }
}